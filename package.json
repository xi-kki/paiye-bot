{
  "name": "paiye-bot",
  "version": "3.0.0",
  "description": "Paiye — AI Career Agent. Remote job search, ATS analysis, interview prep, AI chat on Telegram.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node -c index.js && node -c telegramClient.js && node -c jobEngine.js",
    "syntax": "node -c index.js && node -c telegramClient.js && node -c jobEngine.js && node -c aiChat.js && node -c resumeParser.js && node -c userData.js && node -c cache.js",
    "prepare": "husky"
  },
  "keywords": ["telegram", "bot", "jobs", "career", "ai", "ats"],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.17.0",
    "cheerio": "^1.2.0",
    "dotenv": "^16.6.1",
    "mammoth": "^1.12.0",
    "node-cron": "^4.2.1",
    "pdf-parse": "^2.4.5"
  }
}
